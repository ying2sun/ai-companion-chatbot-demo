# backend/requirements.txt
#
# Trimmed to only what this demo's code actually imports: no database,
# no cloud storage, no auth, no Chinese-text tooling, since none of
# those are part of this build's scope.

fastapi==0.136.1
pydantic==2.13.3
python-multipart
uvicorn[standard]

python-dotenv==1.2.2
httpx==0.28.1
pytz

google-genai>=1.0.0
fastmcp

slowapi

# eval/judge.py only, the chat backend itself uses Gemini, not Claude
anthropic
